package io.huta.segments.player.purchase

import io.vertx.core.Handler
import io.vertx.ext.web.RoutingContext

class PlayerPurchasePost : Handler<RoutingContext> {

    override fun handle(event: RoutingContext) {
    }
}
